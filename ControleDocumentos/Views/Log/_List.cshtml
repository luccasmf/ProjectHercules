@model List<ControleDocumentosLibrary.Logs>
<table class="table table-striped table-bordered commmon-datatable">
    <thead>
        <tr>
            <th>Usuário</th>
            <th>Ação</th>
            <th>Tipo do objeto</th>
            <th>Id do objeto</th>
            <th>Data</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var log in Model)
            {
            <tr>
                <td>@(log.Usuario != null ? log.Usuario.Nome : "")</td>
                <td>@ControleDocumentos.Util.Extension.EnumExtensions.GetEnumDescription(log.Acao)</td>
                <td>@ControleDocumentos.Util.Extension.EnumExtensions.GetEnumDescription(log.TipoObjeto)</td>
                <td>@(log.IdObjeto.HasValue ? log.IdObjeto.ToString() : "")</td>
                <td>@log.Data<td>
            </tr>
        }
    </tbody>
</table>